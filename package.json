{
  "name": "quote-harvester",
  "version": "0.0.1",
  "description": "Harvests quotes from several sources and puts them into a Firebase Database.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^4.0.4",
    "scrape-it": "^3.2.2"
  }
}
